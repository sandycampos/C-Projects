#include"ajuda.c" //só executou incluindo essa linha ou "ajuda.c" no arquivo principal

void mensagem();
int soma(int num1, int num2);
int multiplicacao(int num1, int num2);

/*
    arquivos .h não têm implementação das funções que serão utilizadas
    eles têm, simplesmente, os protótipos de função
    as implementações estão em outro arquivo (ajuda.c)
*/
